#include<iostream>
using namespace std;
int main()
{
    int birth_day = 1;
    int birth_year = 1990;
    string birth_month = "june";

    cout<<"The birth_day is "<< birth_day<<" the year is "<< birth_year<<" the month is "<<birth_month<< endl;

    return 0;



}
